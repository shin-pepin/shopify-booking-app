{
  "name": "booking-validation",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/run.js --bundle --outfile=dist/function.js --format=esm --target=esnext",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@shopify/shopify_function": "^0.3.0"
  }
}

